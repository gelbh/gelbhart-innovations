name: Keep Supabase Alive

on:
  schedule:
    # Run every 2 days to keep Supabase free tier database alive (pauses after 1 week of inactivity)
    - cron: "0 12 */2 * *" # Every 2 days at 12:00 PM UTC
  workflow_dispatch: # Allow manual triggering

jobs:
  ping-supabase:
    name: Ping Supabase REST API
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase REST API
        run: |
          # Determine which secrets to use
          SUPABASE_URL=""
          SUPABASE_KEY=""

          if [ -n "${{ secrets.SUPABASE_URL }}" ] && [ -n "${{ secrets.SUPABASE_ANON_KEY }}" ]; then
            echo "✅ Using SUPABASE_URL and SUPABASE_ANON_KEY secrets"
            SUPABASE_URL="${{ secrets.SUPABASE_URL }}"
            SUPABASE_KEY="${{ secrets.SUPABASE_ANON_KEY }}"
          elif [ -n "${{ secrets.SUPABASE_URL }}" ] && [ -n "${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" ]; then
            echo "✅ Using SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY secrets"
            SUPABASE_URL="${{ secrets.SUPABASE_URL }}"
            SUPABASE_KEY="${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}"
          else
            echo "❌ Required secrets not found!"
            echo "Please set the following secrets in GitHub:"
            echo "  - SUPABASE_URL: Your Supabase project URL (e.g., https://xxxxx.supabase.co)"
            echo "  - SUPABASE_ANON_KEY: Your Supabase anon/public key (preferred)"
            echo "    OR"
            echo "  - SUPABASE_SERVICE_ROLE_KEY: Your Supabase service role key (alternative)"
            echo ""
            echo "💡 Find these in Supabase Dashboard > Settings > API"
            echo "   - Project URL: Settings > API > Project URL"
            echo "   - Anon key: Settings > API > Project API keys > anon public"
            exit 1
          fi

          # Ensure URL doesn't have trailing slash
          SUPABASE_URL=$(echo "$SUPABASE_URL" | sed 's|/$||')

          echo "🔄 Making REST API call to Supabase to register activity..."
          echo "📍 URL: ${SUPABASE_URL}/auth/v1/health"

          # Make a health check request to Supabase Auth API
          # This is a proper health check endpoint that Supabase provides
          # Returns 200 on success and registers as API activity
          RESPONSE=$(curl -s -w "\n%{http_code}" \
            -X GET \
            -H "apikey: ${SUPABASE_KEY}" \
            -H "Authorization: Bearer ${SUPABASE_KEY}" \
            -H "Content-Type: application/json" \
            "${SUPABASE_URL}/auth/v1/health" 2>&1)

          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | sed '$d')

          # Check if request was successful (200 indicates successful health check and API activity)
          if [ "$HTTP_CODE" -eq 200 ]; then
            echo "✅ Successfully made health check request to Supabase"
            echo "✅ HTTP Status: ${HTTP_CODE}"
            echo "✅ API activity registered - this should prevent auto-pausing"
            echo ""
            echo "📊 Response:"
            echo "$BODY"
            echo ""
            echo "💡 This REST API call is recognized by Supabase as activity."
            echo "💡 Check your Supabase Dashboard > Logs > API to verify the request appears."
          else
            echo "❌ Failed to make REST API call to Supabase"
            echo "❌ HTTP Status: ${HTTP_CODE}"
            echo "❌ Response:"
            echo "$BODY"
            echo ""
            echo "💡 Please verify:"
            echo "   1. SUPABASE_URL is correct (format: https://xxxxx.supabase.co)"
            echo "   2. API key is valid and has proper permissions"
            echo "   3. Project is not paused"
            exit 1
          fi
