echo "🔍 Running ERB lint..."
bin/lint

if [ $? -ne 0 ]; then
  echo "❌ ERB lint failed. Fix the errors before pushing."
  exit 1
fi

echo "✅ ERB lint passed"
echo ""
echo "🧪 Running Rails tests..."
ruby bin/rails test

if [ $? -ne 0 ]; then
  echo "❌ Tests failed. Fix the failing tests before pushing."
  exit 1
fi

echo "✅ All tests passed"
echo "🚀 Ready to push!"
