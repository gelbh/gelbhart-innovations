<style>
   .card .position-relative {
    cursor: pointer;
  }
  .swiper-slide img {
    width: 100%;
    height: auto;
    object-fit: cover;
    display: block;
  }
</style>

<!-- Jarallax -->
<%= render 'shared/jarallax' %>

<!-- Breadcrumb -->
<%= render 'shared/breadcrumb' %>

<!-- Brands (Carousel) -->
<section class="container py-2 pb-lg-5 px-5">
  <h2 class="text-center pb-md-2">Our leadership</h2>
  <div class="swiper mx-n2" data-swiper-options='{
    "centerInsufficientSlides": true,
    "slidesPerView": 3,
    "spaceBetween": 40,
    "pagination": {
      "el": ".swiper-pagination",
      "clickable": true
    },
    "breakpoints": {
      "500": {
        "slidesPerView": 3,
        "spaceBetween": 60
      },
      "650": {
        "slidesPerView": 3,
        "spaceBetween": 80
      },
      "900": {
        "slidesPerView": 3,
        "spaceBetween": 100
      },
      "1100": {
        "slidesPerView": 3,
        "spaceBetween": 120
      }
    }
  }'>
    <div class="swiper-wrapper">

      <!-- Team Members -->
      <% for member in @members do %>
        <div class="swiper-slide py-3 px-2 card border-0 bg-transparent">
          <div class="position-relative card-hover" data-bs-toggle="modal" data-bs-target="#<%= member[0].split(' ').first %>">
            <img src="<%= asset_path(member[0].split[0].downcase) %>" class="rounded-3" alt="<%= member[0] %>">
            <div class="card-img-overlay d-flex flex-column align-items-center justify-content-center rounded-3" data-bs-toggle="tooltip" data-bs-placement="top" title="Click me for more info!">
              <span class="position-absolute top-0 start-0 w-100 h-100 bg-dark opacity-50 rounded-3"></span>
              <div class="position-relative d-flex zindex-2">
                <a href="<%= member[2] %>" target="_blank" class="btn btn-icon btn-secondary btn-linkedin btn-sm bg-white me-2">
                  <i class="bx bxl-linkedin"></i>
                </a>
              </div>
            </div>
          </div>
          <div class="card-body text-center px-4 py-3 d-flex flex-column align-items-center">
            <h3 class="fs-lg fw-semibold pt-1 mb-2 text-center" style="word-break: normal; overflow-wrap: break-word; white-space: normal;"><%= member[0] %></h3>
          </div>
        </div>
      <% end %>

    </div>
    <!-- Pagination (bullets) -->
    <div class="swiper-pagination position-relative pt-2 mt-4"></div>
  </div>

  <!-- Team Modals -->
  <% for member in @members do %>
    <div class="modal fade" id="<%= member[0].split(' ').first %>" tabindex="-1" role="dialog">
      <div class="modal-dialog" role="document">
        <div class="modal-content">
          <div class="modal-header row">
            <div class="col-6 text-start">
              <h5 class="modal-title"><%= member[0] %></h5>
            </div>
            <div class="col-6 text-end">
              <img src="<%= asset_path(member[0].split[0].downcase) %>" class="rounded-3" alt="<%= member[0] %>" width="100">
            </div>
          </div>
          <div class="modal-body"><p><%= member[1] %></p></div>
        </div>
      </div>
    </div>
  <% end %>

</section>